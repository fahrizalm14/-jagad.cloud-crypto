{
    "name": "@jagad.cloud/crypto",
    "version": "0.1.0",
    "description": "Browser-safe helpers for password-derived keys, keypair generation, and private-key encryption using Web Crypto API.",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -p .",
        "test": "jest",
        "test:c": "jest --coverage",
        "lint": "eslint . --ext .ts",
        "prepare": "npm run build",
        "console": "tsx -r fake-indexeddb/auto src/console.ts --watch"
    },
    "devDependencies": {
        "@types/jest": "^29.5.2",
        "fake-indexeddb": "^6.2.2",
        "jest": "^29.6.1",
        "ts-jest": "^29.1.0",
        "tsx": "^4.20.5",
        "typescript": "^5.2.2"
    },
    "dependencies": {
        "idb": "^8.0.3"
    }
}